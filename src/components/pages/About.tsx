import React, { memo, FC } from "react";
import Header from "../template/Header";

const About: FC = memo(() => {
  return (
    <>
      <h1>Aboutページです</h1>
      <Header />
    </>
  );
});

export default About;
